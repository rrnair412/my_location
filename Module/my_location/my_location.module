<?php

/**
 * Implements hook_theme().
 */
function my_location_theme($existing, $type, $theme, $path) {
  return [
    'my-location' => [
      'variables' => ['date' => NULL, 'timezone' => NULL],
    ],
  ];
}